Simple DMA test: copy qwords from a 64-bit RNG into host memory.

quartus_sh --flow compile gt.qpf -c gt
quartus_pgm --mode=JTAG -o 'p;gt-out/gt.sof'

# Generate .pof:
quartus_cpf -c gt.cof

# Actually program the flash (need a JTAG-program to crash the host and force reconfig-from-flash)
quartus_pgm --mode=JTAG gt.cdf
